import 'package:flutter/material.dart';

class BadgesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Badges'),
        backgroundColor: const Color(0xFF146C94),
      ),
      body: Center(
        child: Text('Badges Page Content'),
      ),
    );
  }
}
